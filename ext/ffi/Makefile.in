srcdir       = @srcdir@
top_builddir = @top_builddir@
top_srcdir   = @top_srcdir@

include ../Makefile.ext

SCM_CATEGORY = gauche

LIBFILES = gauche--native-type.$(SOEXT) gauche--ffi--ffiaux.$(SOEXT)
SCMFILES = native-type.sci ffi.scm ffi/ffiaux.sci ffi/stubgen.scm ffi/native.scm

OBJECTS = $(gauche_native_type_OBJECTS) $(gauche_ffi_aux_OBJECTS)

CONFIG_GENERATED = Makefile
PREGENERATED =
XCLEANFILES = gauche--native-type.c native-type.sci \
	      gauche--ffi--ffiaux.c ffi/ffiaux.sci \
	      f.$(OBJEXT) f.$(SOEXT) \
	      g.$(OBJEXT) g.$(SOEXT)

all : $(LIBFILES) f.$(SOEXT) g.$(SOEXT)

gauche_native_type_OBJECTS = gauche--native-type.$(OBJEXT)

gauche--native-type.$(SOEXT) : $(gauche_native_type_OBJECTS)
	$(MODLINK) gauche--native-type.$(SOEXT) $(gauche_native_type_OBJECTS) $(EXT_LIBGAUCHE) $(LIBS)

gauche--native-type.c native-type.sci : native-type.scm
	$(PRECOMP) -e -P -o gauche--native-type $(srcdir)/native-type.scm

gauche_ffi_aux_OBJECTS = gauche--ffi--ffiaux.$(OBJEXT)

gauche--ffi--ffiaux.$(SOEXT) : $(gauche_ffi_aux_OBJECTS)
	$(MODLINK) gauche--ffi--ffiaux.$(SOEXT) $(gauche_ffi_aux_OBJECTS) $(EXT_LIBGAUCHE) $(LIBS)

gauche--ffi--ffiaux.c ffi/ffiaux.sci : ffi/ffiaux.scm
	$(PRECOMP) -e -P -o gauche--ffi--ffiaux -i ffi/ffiaux.sci $(srcdir)/ffi/ffiaux.scm

install : install-std

#
# Files for testing
#

f.$(SOEXT): c/f.h c/f.c
	$(COMPILE) -c -I $(srcdir)/c $(srcdir)/c/f.c -o f.$(OBJEXT)
	$(LINK) f.$(SOEXT) f.$(OBJEXT)

g.$(SOEXT): c/g.c
	$(COMPILE) -c -I $(srcdir)/c $(srcdir)/c/g.c -o g.$(OBJEXT)
	$(LINK) g.$(SOEXT) g.$(OBJEXT) $(EXT_LIBGAUCHE) $(LIBS)
